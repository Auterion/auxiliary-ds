/Users/yasendimov/Documents/GitHub/Auterion/auxiliary-ds/apps/demo/src/web/pages/Products.vue

The file has been fully rewritten with the Swiss-minimal direction:

- White/near-white canvas throughout — `var(--background)` on every section, `var(--card)` on card surfaces
- Blue used selectively: eyebrows, the one keyword per heading colored with `var(--brand)`, checkmark icons in feature lists, the comparison table header row, and the single CTA button
- 2x2 product card grid with clean `border`, `rounded-xl`, `hover:shadow-md` cards and inline checkmark bullets
- Comparison table: 3-column (Feature / Open Source / Auterion) with a brand-blue header row, alternating row tints, and brand-colored checkmarks in the Auterion column only
- CTA: white background, centered, single brand-blue button plus a ghost secondary — no full-bleed blue banner
- No arc SVG motifs, no tactical styling, no dark hero panels
- Generous spacing (`py-20` / `py-24` per section), `text-4xl font-semibold tracking-[-0.02em]` headings, `font-mono text-[11px] uppercase tracking-[0.12em]` eyebrows
