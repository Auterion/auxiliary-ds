{
  "name": "@auxiliary/demo",
  "version": "0.0.0",
  "description": "Auxiliary system showcase — theme switcher, status grid, typography specimens, component matrix. Living artifact for design + engineering review.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "preview": "vite preview --port 5174",
    "lint": "echo \"@auxiliary/demo lint — TODO\" && exit 0",
    "typecheck": "vue-tsc --noEmit",
    "test": "echo \"@auxiliary/demo test — TODO\" && exit 0"
  },
  "dependencies": {
    "@auxiliary/css": "workspace:*",
    "@auxiliary/icons": "workspace:*",
    "@auxiliary/tokens": "workspace:*",
    "@auxiliary/vue": "workspace:*",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.0",
    "vue-tsc": "^2.2.0"
  }
}
