{
  "name": "@auxiliary/tokens",
  "version": "0.0.0",
  "description": "Auxiliary design tokens — DTCG-spec JSON, the source of truth for every other package.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/tokens.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./dist/tokens.ts",
    "./dist/tailwind-v4.css": "./dist/tailwind-v4.css",
    "./dist/tokens.css": "./dist/tokens.css",
    "./dist/figma.tokens.json": "./dist/figma.tokens.json"
  },
  "scripts": {
    "build": "node build.mjs",
    "lint": "echo \"@auxiliary/tokens lint — no sources yet\" && exit 0",
    "typecheck": "echo \"@auxiliary/tokens typecheck — no sources yet\" && exit 0",
    "test": "echo \"@auxiliary/tokens test — no sources yet\" && exit 0"
  },
  "devDependencies": {
    "culori": "^4.0.2",
    "style-dictionary": "^5.4.1"
  }
}
