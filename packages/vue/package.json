{
  "name": "@auxiliary/vue",
  "version": "0.0.0",
  "description": "Vue 3 primitives for Auxiliary, styled via @auxiliary/css recipes and utilities.",
  "license": "UNLICENSED",
  "type": "module",
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/auxiliary-vue.js"
    }
  },
  "scripts": {
    "build": "vite build && vue-tsc -p tsconfig.build.json",
    "lint": "echo \"@auxiliary/vue lint — TODO\" && exit 0",
    "typecheck": "vue-tsc -p tsconfig.build.json --noEmit",
    "test": "echo \"@auxiliary/vue test — TODO\" && exit 0"
  },
  "dependencies": {
    "@auxiliary/css": "workspace:*",
    "@auxiliary/tokens": "workspace:*"
  },
  "peerDependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.0.0",
    "vue": "^3.5.0",
    "vue-tsc": "^2.2.0"
  }
}
