{
  "name": "@auxiliary/figma-sync",
  "version": "0.0.0",
  "description": "Reserved slot for Figma sync tooling. Parked — see README.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"@auxiliary/figma-sync — parked, see README\" && exit 0",
    "lint": "echo \"@auxiliary/figma-sync — parked\" && exit 0",
    "typecheck": "echo \"@auxiliary/figma-sync — parked\" && exit 0",
    "test": "echo \"@auxiliary/figma-sync — parked\" && exit 0"
  }
}
