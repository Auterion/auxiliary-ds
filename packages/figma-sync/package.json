{
  "name": "@auxiliary/figma-sync",
  "version": "0.0.0",
  "description": "One-way, session-triggered sync of Auxiliary tokens into Figma Variables via the Figma MCP.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/build-program.mjs",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "echo \"@auxiliary/figma-sync — JS only, no typecheck\" && exit 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@auxiliary/tokens": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
