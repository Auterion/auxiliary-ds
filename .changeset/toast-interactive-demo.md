---
---

Docs: the Toast page's "Driving toasts from state" example is now a live, interactive demo. Three buttons (info / success / alarm) drive a single `<Toast>` whose title and description swap per variant, mirroring the demo playground — replacing the previous code-only snippet. Markdown-only; no package changes.
