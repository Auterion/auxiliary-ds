---
---

Docs (Phase 6.4 slice 4): add the **Marketing blocks** pattern — the expressive-register brand surfaces carrying the operational design language (mono type, instrument framing). Hero statement, `MarketingStatBar` (proof points as instrument readouts), `MarketingPillars` (dashboard-widget grammar), `MarketingValueGrid` (structured matrix), `MarketingQuote` (mission-log attribution), CTA band. Composed from existing primitives; added to the Patterns catalog + sidebar. No package changes.
