---
---

Docs (Phase 6.5 slice 6): add a **Data viz** section to the docs — a palette overview (categorical / sequential / diverging swatches + the status-reservation/CVD rationale) and live pages for **Sparkline**, **Gauge**, and a streaming **Time series** (uPlot, `<ClientOnly>`, with a 10 Hz stream toggle). Registers the `@auxiliary/viz` chart components in the docs theme and adds the top-nav + sidebar section. No package changes.
