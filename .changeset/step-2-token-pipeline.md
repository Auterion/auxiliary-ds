---
"@auxiliary/tokens": minor
---

Wire the Step 2 token pipeline: Style Dictionary v5 emits four artifacts (`tailwind-v4.css`, `tokens.css`, `tokens.ts`, `figma.tokens.json`) from DTCG 2025.10 source files. Seeded with one token per type (`color.primitive.gray.900`, `spacing.4`, `radius.md`) to prove the round-trip; Step 3 authors the full primitive + semantic layers.
