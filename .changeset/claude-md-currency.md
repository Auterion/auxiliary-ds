---
---

Docs: refresh `CLAUDE.md` for concepts added since it was written — the orthogonal `[data-theme]` / `[data-register]` token-mode axes (expressive vs operational), the `@auxiliary/css/format` subpath (lat/long·MGRS, units, locale numbers), a pointer to `ROADMAP.md` (forward source of truth) + `.claude/docs/` design grounding, and the `level` (operational severity) vs `variant` (design treatment) convention. No package changes.
