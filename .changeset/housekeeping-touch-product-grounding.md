---
---

Docs/planning: add a touch/mobile posture decision and durable real-product grounding ahead of Phase 6.4/6.5. New `ROADMAP.md` `§ Input modality & touch` decision record promotes input modality to a first-class axis (`@media (pointer:coarse)` + `[data-input]` override, a `--target-min` 44px touch floor that composes *over* register density, gated like register orthogonality) — a 6.2-spine-extension slice and prerequisite for §6.4. New `.claude/docs/auterion-product-inventory.md` captures the Mission Control / AuterionSuite / AuterionOS screenshots durably: context-level map, observed-pattern→status table, the maximal-minus-pipelines scope boundary (incl. the parked satellite/terrain-basemap legibility constraint), and a just-in-time repo-scan protocol. §6.4 gains a repo-scan step, an EntityIcon owner (slice 6), and a mission-critical instruments slice; §6.5 notes the chart set is confirmed by real product use; §6.1 backlog gains UsageMeter, CopyField, EditableField, FileUpload. No package changes.
