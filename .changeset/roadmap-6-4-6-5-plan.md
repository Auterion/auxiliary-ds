---
---

Docs/planning: add the detailed §6.4 (Compose — blocks, page templates, interaction depth) and §6.5 (Data-viz — token-driven, theme- & CVD-safe viz layer) sections to `ROADMAP.md`, expanding the one-line sub-phase rows into sliced, PR-sized execution plans grounded in the existing demo compositions and the benchmark research. Each section flags its one open decision (blocks home; charting engine + package) rather than pre-deciding it. No package changes.
