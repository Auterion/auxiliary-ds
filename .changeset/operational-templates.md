---
---

Docs (Phase 6.4 slice 8, completing): add the remaining operational templates — **Mission planning** (map + dashed path overlay through numbered waypoints, waypoint list, mission params), **Fleet overview** (status roll-up + a responsive card-per-vehicle grid, alarm-in-periphery), and **Post-flight review** (headline metrics, altitude-profile sparkline, severity-tagged event log). Copy-able assemblies of existing blocks + viz; added to the Templates index + sidebar. No package changes.
