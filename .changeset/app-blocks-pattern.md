---
---

Docs (Phase 6.4 slice 5): add the **App blocks** pattern — the Level-2 conventional app pieces, composed from existing primitives. Command palette (`Dialog` + filter `Input` + list), filter bar (`DropdownMenu` + removable `Badge` chips), data-table + toolbar (search/columns/actions over `Table`), notifications center (`Popover` off a bell), detail drawer (slide-in; compose `Dialog` in production), and an empty/onboarding state. Grounded in an AuterionSuite scan (EmptyState confirmed in-product). New primitives the scan surfaced (CopyField/EditableField/FileUpload/UsageMeter) stay in the §6.1 backlog. No package changes.
