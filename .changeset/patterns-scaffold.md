---
---

Docs (Phase 6.4 slice 1): add a **Patterns** section to the docs — copy-able compositions of primitives, sitting alongside Components. Records the blocks-home decision (docs-first; no `@auxiliary/blocks` package yet) and migrates the demo's existing operational compositions as the first four documented blocks: Fleet table, Telemetry grid, Vehicle status card, Pre-flight checklist. Each shows a live preview, idiomatic source, and composition/a11y notes. New `apps/docs/patterns/` pages + nav (top-nav + sidebar group). No package changes.
