---
---

Docs (Phase 6.4 slice 3): add the **Motion** foundations page — motion as signal, not flourish. Ties the `--duration-*` (120/200/320ms expressive → 80/120/200ms operational) and `--ease-*` tokens to a purpose model (micro ≤150ms; surface transitions; flight-critical 0–80ms; expressive earns motion), and reconciles register motion (a design choice) with the global `prefers-reduced-motion` reset (an a11y override that always wins). Includes a live duration demo and do/don't. Added to the Foundations sidebar. No token churn, no package changes.
