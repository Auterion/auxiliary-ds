---
---

Docs: refresh `CLAUDE.md` to describe the now-scaffolded monorepo (real `@auxiliary/*` package names, Node 24+/pnpm 11+, root + package-scoped commands, and CI gotchas), and correct the stale version string in `README.md`. No package changes.
