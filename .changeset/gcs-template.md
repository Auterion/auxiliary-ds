---
---

Docs (Phase 6.4 slice 8): add the **Templates** section and the **GCS layout** template — the [operational console blocks](/patterns/operational-console) assembled into the full ground-control-station screen (status bar, fleet, map, inspector, mission timeline + alert annunciator) in the operational register on a dark theme. Air-gap / degraded connectivity is a first-class state: a link-loss toggle shows the screen's stale-data treatment (link-lost badge, dimmed vitals, "telemetry stale — last known positions" on the map). No package changes.
