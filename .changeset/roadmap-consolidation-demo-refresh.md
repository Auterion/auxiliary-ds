---
---

Consolidate the four roadmap files into a single `ROADMAP.md` (folding in the Phase 6 sub-roadmap, the 6.1 component audit, and the 6g register decision as in-document sections; delete the fragments) and refresh `apps/demo` to mount the full current component set — adds Combobox, NumberField, and Table sections plus `invalid` / `size` validation showcases and a live `Skeleton.loading` toggle. Docs + demo only — no package, API, or token changes.
