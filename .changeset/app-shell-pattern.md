---
---

Docs (Phase 6.4 slice 2): add the **App shell** pattern — the top-bar + collapsible-sidebar + content frame every Level-2/3 surface sits in (grounded in AuterionOS / AuterionSuite). Live, interactive demo (collapse-to-icons, active nav, org-switcher `DropdownMenu`) composing existing primitives + CSS-grid layout, with the no-full-page-scroll rule, responsive/keyboard notes, and the docs-first decision (no `<AppShell>` component yet — Principle 3). Added to the Patterns catalog + sidebar. No package changes.
